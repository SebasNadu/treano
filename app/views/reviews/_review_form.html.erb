 <%= simple_form_for([reviewable, review],
    data: {
      insert_in_list_target: "form",
      action: "submit->insert-in-list#send"
      }) do |f| %>
    <%= f.input :rating %>
    <%= f.input :content %>
    <%= f.submit "Add a review", class: "btn btn-primary" %>
  <% end %>
