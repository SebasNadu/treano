  <%= simple_form_for [listable, list_item] do |f| %>
    <%= f.association :list, collection: lists, label_method: :list_name, value_method: :id %>
    <%= f.submit "Add to list", class: "btn btn-primary" %>
  <% end %>
